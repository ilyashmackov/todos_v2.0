import React, { useState, useEffect } from "react";
import { v4 as uuidv4 } from "uuid";

const ChangeData = () => {
  // const OnDelete = (id:any) => {
  //     console.log(id);
  //     const index = items.findIndex(elem => elem.id === id);
  //     console.log(index);
  //     const newArr = [...items.slice(0, index), ...items.slice(index + 1)];
  //     setItems(newArr);
  // }
  // const AddItem = (task:any) => {
  //     console.log(task);
  // }
  // return {items, GetData, OnDelete}
  //
};

export default ChangeData;
